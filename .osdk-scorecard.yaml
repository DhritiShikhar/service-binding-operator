scorecard:
  # Setting a global scorecard option
  output: json
  plugins:
    # `basic` tests configured to test 2 CRs
    - basic:
        cr-manifest:
          - "deploy/crds/apps_v1alpha1_servicebindingrequest_cr.yaml"
    # `olm` tests configured to test 2 CRs
    - olm:
        cr-manifest:
          - "deploy/crds/apps_v1alpha1_servicebindingrequest_cr.yaml"
        csv-path: "deploy/olm-catalog/service-binding-operator/0.1.0-267/service-binding-operator.v0.1.0-267.clusterserviceversion.yaml"
